# Project Name: Git-task 
# Description: This was written to push my code from git-task repository to this remote repository. 

# code for push 
git remote add origin https://github.com/BridgetMary22/git-task.git
git branch -M main
git push -u origin main
